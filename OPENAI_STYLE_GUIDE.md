# OpenAI 风格设计规范

本项目的界面风格参考 OpenAI / ChatGPT 的产品气质：克制、中性、清晰、工具优先。所有页面、弹窗、按钮、画布节点、聊天区、侧边栏和设置页的视觉改动都必须先对照本文档。

参考依据：[OpenAI Design Guidelines](https://openai.com/brand/)。官方规范强调品牌元素要保持固定比例、留白和清晰层级，不随意加颜色、纹理、特效或变形；字体气质是几何、功能性、圆润亲和。本文档把这些原则转成当前项目可执行的界面规则。

## 核心原则

1. 中性色优先：黑、白、灰是主色。不要把绿色、紫色、蓝色等彩色当成常驻状态色或主按钮色。
2. 内容优先：界面像工具，不像营销页。不要用强装饰、彩色光晕、大面积渐变、玻璃拟态。
3. 状态靠层级表达：选中、启用、聚焦用边框、明度、背景层级表达；错误可以用红色；普通成功也尽量用中性提示。
4. 几何但柔和：圆角要克制，组件看起来是精密工具，不是玩具胶囊。
5. 信息密度适中：桌面工具允许紧凑，但文字、按钮、输入框必须有稳定尺寸，不挤、不跳、不换行破坏布局。

## 颜色系统

### 深色主题

- `--bg`: `#101010` 页面底色。
- `--panel`: `#151515` 主面板。
- `--panel-2`: `#1a1a1a` 悬停、次级面。
- `--surface`: `#1f1f1f` 输入框、浮层、卡片面。
- `--line`: `#2f2f2f` 默认边框。
- `--line-strong`: `#4a4a4a` 强边框、聚焦边框。
- `--text`: `#ededed` 主文字、主按钮底色。
- `--muted`: `#9b9b9b` 次级文字、图标。
- `--faint`: `#6f6f6f` 弱文字。
- `--accent`: `#f2f2f2` 中性状态色，用于启用、已选、连接高亮。
- `--danger`: `#ff6b6b` 错误、删除、失败。

### 浅色主题

- `--bg`: `#f5f5f4`
- `--panel`: `#fbfbfa`
- `--panel-2`: `#f1f1f0`
- `--surface`: `#ffffff`
- `--line`: `#dededc`
- `--line-strong`: `#bdbdb9`
- `--text`: `#1f1f1d`
- `--muted`: `#70706b`
- `--faint`: `#989891`
- `--accent`: `#1f1f1d`
- `--danger`: `#c43c3c`

### 禁用颜色

- 常驻 UI 状态禁止使用绿色：包括开关 ON、启用模型、选中服务商、连接线、版本更新点、附件标签、画布选中态。
- 禁止紫色/蓝紫色光晕、彩色外发光、霓虹色。
- 禁止大面积彩色渐变。
- 用户可选的画布颜色可以保留多色，但默认值和系统状态不使用绿色。

## 背景与层级

- 页面背景使用 `--bg` 或 `--canvas`。
- 工作台/侧边栏/聊天栏使用 `--panel`。
- 输入框、弹窗、浮层、卡片使用 `--surface`。
- 悬停使用 `--panel-2`。
- 选中使用中性灰：深色 `#303030` + `#7a7a7a` 边框；浅色 `#e7e7e5` + `#858580` 边框。
- 浮层阴影只用于弹窗、菜单、预览层，避免彩色阴影。

## 圆角

- 普通按钮、菜单项、输入框：`6px` 到 `10px`。
- 弹窗、主要面板：`10px` 到 `12px`。
- 小标签、开关、发送按钮可以使用 `999px`，但只限真实 pill/圆形控件。
- 不要把所有容器都做成大圆角卡片；页面区域用线和间距分组。

## 按钮

- 图标按钮默认点击区不小于 `30px`；设置页、弹窗和表单关键操作不小于 `40px`。
- 图标默认 `16px`；关键操作图标 `18px` 到 `20px`。
- 主按钮：白底黑字（深色主题）或黑底白字（浅色主题），只用于明确提交动作。
- 次按钮：透明底、灰边框、灰文字；悬停时 `--panel-2`。
- 删除/失败按钮只在文字、边框或悬停状态使用 `--danger`，不要整块红底。

## 开关与状态

- 开关 ON：轨道使用中性灰，圆点使用白/近白；不得使用绿色。
- 状态标签 ON/OFF：边框 pill；ON 使用中性高亮文字和边框；OFF 使用 `--muted`。
- 勾选态：使用 `--accent`，不要使用绿色。
- 成功提示：优先使用中性提示；只有明确、短暂、非品牌主视觉的系统结果可少量使用 `--accent`，不得用绿色。
- 错误提示：使用 `--danger`。

## 输入框与表单

- Label 在输入框上方。
- 输入框高度 `44px`，边框 `--line`，聚焦 `--line-strong` + `--focus`。
- 输入框内部按钮不额外加独立边框，只有必要时用 1px 分割线。
- 下拉框箭头右侧至少留 `14px`，文本右侧留足 `44px`。

## 弹窗

- 弹窗背景 `--panel`，边框 `--line`，圆角 `10px` 到 `12px`。
- 标题行高度稳定，关闭按钮点击区 `40px`。
- 弹窗按钮右对齐，主按钮只用中性高对比色。
- 弹窗不得出现绿色确认按钮或绿色边框。

## 侧边栏

- 侧边栏以列表和线分组为主，不做营销式卡片。
- 当前工作台、当前话题、当前服务商都用灰色选中态。
- 版本更新点使用中性高亮点，不使用绿色点。
- Logo 和品牌区域不得使用紫色/彩色光晕。

## 聊天区

- 用户输入区为稳定输入面，按钮遵循图标按钮规范。
- 附件标签、工具菜单、复制/导出成功提示使用中性色。
- AI 回复的导出、复制、操作菜单使用统一菜单样式：`--surface` 背景、`--line-strong` 边框、`6px` 到 `10px` 圆角。

## 画布

- 画布背景使用深色点阵或浅色点阵，不加彩色光影。
- 节点选中、连接线、连接手柄默认使用 `--accent`，不使用绿色。
- 用户主动选择颜色的内容可以有多色；系统默认画笔、默认文本色和默认背景色不使用绿色。
- 图片选中框、节点 resize handle 使用中性高亮。

## 字体与文字

- 使用系统 sans-serif 栈，保持 OpenAI 式克制工具感。
- 标题靠字重和层级区分，不靠超大字号。
- 字间距保持 `0`，不要负字距。
- 列表文本必须单行省略或稳定换行，不允许因为侧栏变窄导致容器高度乱跳。

## 动效

- 动效只用于 hover、active、弹窗出现、菜单展开等低成本反馈。
- 只动画 `opacity` 和 `transform`，不动画布局属性。
- 不使用弹跳、炫技、彩色光效。

## 交付检查

涉及 UI/UX、页面风格、颜色、按钮、圆角、背景、图标、输入框、弹窗、画布或聊天视觉的改动，交付前必须检查：

- 没有新增绿色常驻状态。
- 没有新增紫色/彩色光晕。
- 主按钮、次按钮、图标按钮尺寸符合本文档。
- 圆角没有脱离组件等级。
- 深色和浅色主题都能读清。
- `pnpm lint`、`pnpm build`、`pnpm test:coverage` 通过。
