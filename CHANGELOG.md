# Changelog

## v0.1.4

### 新功能

- 新增提示词卡片 Markdown 标题折叠状态持久化，切换卡片、画布或重新打开后能恢复用户的折叠选择。
- 新增卡片级折叠状态字段，支持显式空折叠状态覆盖默认折叠规则。

### 修复

- 修复不同画布或不同提示词卡片之间可能串用标题折叠状态的问题。
- 修复编辑 Markdown 后折叠标题 ID 没有同步重映射并保存的问题。

## v0.1.3

### 新功能

- 新增聊天面板会话级状态管理，按话题保存折叠、宽度、对比开关、对比卡片和对比面板状态。
- 新增对比面板卡片作用域逻辑，切换话题或复制话题时能保留更准确的对比配置。
- 抽离聊天面板本地存储逻辑，降低 App 组合层复杂度。

### 修复

- 修复对比面板在话题切换、复制和导入后可能引用错误卡片的问题。
- 修复画布连线映射、选择、删除和空白处创建节点的若干边界问题。
- 修复话题导入导出后对比面板状态和卡片作用域不一致的问题。

## v0.1.2

### 新功能

- 新增流程图生成能力，可从提示词/文本生成画布流程节点。
- 新增画布工作区控制组件，整理画布工具栏和生成入口。
- 工作区导入导出支持默认模型设置载荷，便于迁移完整配置。
- 默认模型设置增强，支持更多供应商与模型字段的归一化。

### 修复

- 修复话题作用域内画布元素筛选、复制和旧数据修复的边界问题。
- 修复侧边栏会话与画布排序在部分旧数据下不稳定的问题。
- 修复提示词卡片默认折叠标题和 Markdown 预览的若干一致性问题。

## v0.1.1

### 新功能

- 新增统一 Markdown 渲染器，支持 GitHub Flavored Markdown。
- 聊天消息、对比回复、提示词卡片预览和 Markdown 预览弹窗统一使用新的 Markdown 渲染能力。

### 修复

- 修复表格、任务列表、删除线等 Markdown 内容在不同预览入口表现不一致的问题。
- 优化 Markdown 内容样式，提高聊天区和提示词预览中的可读性。

## v0.1.0

### 新功能

- 新增选中文字放大镜设置，可在其他设置中开关并配置显示方式。
- 新增聊天话题顺序调整能力，侧边栏话题可以按用户调整后的顺序展示。
- 优化聊天输入区粘贴和提交体验。

### 修复

- 修复画布剪贴板在部分粘贴场景下没有正确处理选择内容的问题。
- 修复聊天话题重排后排序值可能不稳定的问题。
- 修复设置弹窗缺少其他设置入口的问题。

## v0.0.9

### 新功能

- 增加话题级画布范围字段，提示词卡片、形状、图片、手写线条、文本节点和连线可以归属到具体聊天话题。
- 支持导出/导入带画布内容的话题包，并保留对比面板的隐藏子会话。
- 新增聊天话题复制、排序、当前话题恢复、画布视口持久化和更细的画布记录作用域。

### 修复

- 修复旧话题复制后画布元素可能串到其他话题的问题。
- 修复删除话题时隐藏对比会话和消息没有一起清理的问题。
- 修复旧数据缺少 `topicSessionId` 时话题画布范围识别不稳定的问题。

### 迁移

- IndexedDB schema 升级到版本 7，新增 `chatSessions.parentSessionId`、`chatSessions.hidden` 索引。
- 应用启动后会自动扫描旧话题，并为可匹配的旧画布记录补齐 `topicSessionId`。
- 更新按钮在后续版本中会等待本地服务重启后自动刷新页面，确保新版前端加载并触发 IndexedDB 升级。
